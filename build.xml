<?xml version="1.0" encoding="utf-8"?>
<project name="vmware-cli" default="jar" basedir=".">

  <property name="src.dir"   value="src"/>
  <property name="lib.dir"   value="lib"/>
  <property name="build.dir" value="build"/>
  <property name="dist.dir"  value="dist"/>

  <path id="classpath">
    <fileset dir="${lib.dir}">
      <include name="**/*.jar"/>
    </fileset>
  </path>

  <target name="clean">
    <delete dir="${build.dir}"/>
    <delete dir="${dist.dir}"/>
  </target>

  <target name="compile">
    <mkdir dir="${build.dir}"/>
    <javac srcdir="${src.dir}" destdir="${build.dir}"
           includeantruntime="false"
           source="1.6" target="1.6"
           classpathref="classpath"/>
  </target>

  <target name="jar" depends="compile">
    <mkdir dir="${dist.dir}"/>
    <jar destfile="${dist.dir}/vmware-cli.jar" basedir="${build.dir}">
      <manifest>
        <attribute name="Main-Class" value="com.emresavas.vmwarecli.Main"/>
        <attribute name="Class-Path" value="../lib/vim25.jar"/>
      </manifest>
    </jar>
  </target>

</project>
